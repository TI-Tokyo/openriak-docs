# Hugo site upgrade report

Generated: 2026-08-28 (Asia/Tokyo)

## Outcome

The old Hugo site's presentation and documentation-site behaviour have been rebuilt around Hugo 0.165.0 and applied to the new OpenRiak Markdown tree at `C:\Users\pjacl\Downloads\openriak-docs`.

The source at `C:\Users\pjacl\Downloads\hugo-site` was used only for inspection and for copying three branding assets. Its `content` directory was not copied or modified.

## Version upgrade

The old project contained three inconsistent Hugo requirements:

- Hugo 0.18.1 in `.travis.yml`.
- Hugo 0.25.1 in `docker/Dockerfile`.
- A minimum of Hugo 0.16 in `Rakefile`.

The replacement pins Hugo 0.165.0 in `.hugo-version`, `Dockerfile`, and `compose.yaml`. The official Windows extended binary was downloaded to a temporary directory, verified against the release checksum, and used for the validation build.

- Verified executable: `hugo v0.165.0-76a5e1880ab46688155b02e99bab9be2a6134492+extended windows/amd64`
- Verified archive SHA-256: `C0259EB25D11B7D1EE7CD2F86DA84F17323A36A90DEE0AC0F7A93B635EC837F9`
- Release: <https://github.com/gohugoio/hugo/releases/tag/v0.165.0>

## What was retained

- The recognizable Riak orange, cyan, green, and blue palette.
- A fixed documentation header, version picker, hierarchical navigation, search, page metadata, and community links.
- The favicon and two Riak logo assets from the old site's `static/images/branding` directory.
- The historical Bitcask capacity calculator, ported to dependency-free JavaScript with clearer validation and planning caveats.

## What was replaced

- Blackfriday configuration was replaced by Goldmark configuration. Raw HTML is enabled because the trusted migrated Markdown contains legacy HTML tables, inline elements, and the capacity calculator.
- Hugo 0.18-era Scratch-driven templates and generated front-matter menus were replaced by Hugo's current `baseof.html`, `home.html`, `page.html`, `section.html`, `_partials`, `_markup`, and `_shortcodes` structure.
- The Ruby, Rake, Sass 3, Sprockets, CoffeeScript, Uglifier, Bootstrap alpha, jQuery, and Lunr build chain was removed from the applied site. The replacement uses plain CSS and vanilla JavaScript and has no Node or Ruby dependency.
- The old all-pages sidebar was changed to render the four Diátaxis roots plus only the active branch, preventing navigation markup from multiplying as historic versions are added.
- Search is generated by Hugo as `search-index.json` and filtered in the browser to the selected version.

## Files added

- `hugo.yaml`, `.hugo-version`, `.gitignore`, `README.md`
- Current Hugo templates under `layouts`
- Plain site assets under `static/css` and `static/js`
- Three copied branding assets under `static/images/branding`
- `Dockerfile` and `compose.yaml`, plus the restored `docker/run.local.sh`, `docker/run.local.ps1`, and `docker/docker-compose.localhost-preview.yaml` HTTP preview wrapper
- `tools/scripts/build.ps1`, `tools/scripts/build.sh`, and `tools/scripts/validate-links.ps1`

No Docusaurus, MDX, package manifest, Ruby bundle, generated `public` directory, or old Hugo `content` tree was added.

## Build validation

The complete draft-inclusive content tree was built with Hugo 0.165.0 and `--panicOnWarning`.

- Hugo warnings: 0
- Hugo build errors: 0
- Pages reported by Hugo: 789
- HTML files: 786
- Total generated files: 844
- Static files: 54
- Search records: 689
- Generated HTML size after bounded navigation: 17.05 MiB
- `/kv/3.4.0/`: generated
- `/kv/3.4.1/`: generated

All migrated pages remain `draft: true`, so preview and editorial builds must use `--buildDrafts`. Publication builds will omit them until their technical reviews are complete.

## Rendered-link findings

The rendered build exposed six malformed legacy links that were not visible to the earlier Markdown-only graph review. These were repaired in both 3.4.0 and 3.4.1:

- Four obsolete encoded `baseurl` links now target each version's strong-consistency configuration section.
- Two malformed `riak admin` links now use the existing Markdown reference definition.

The deeper rendered-output scan then found 989 unresolved local references in the migrated content: 495 from 3.4.0 and 494 from 3.4.1; 570 use root-relative paths and 419 use relative paths. These are content-migration issues rather than Hugo/template failures. They include links to legacy route names, pages not yet represented in the Diátaxis tree, and some legacy HTML examples that are being interpreted as live markup.

The complete machine-readable list is in `hugo-link-validation.json`. This rendered-output result supersedes the link portion of the earlier `site-integration-review`, which explicitly stated that no Hugo project had yet been available for a final build.

## Deployment notes

- `baseURL` currently defaults to `https://www.openriak.org/docs/`; override it at build time if the final host differs.
- `docker compose up` or either `docker/run.local` wrapper serves the draft-inclusive site over plain HTTP at `http://localhost:1314/docs/`, bound only to `127.0.0.1`.
- The Docker image build includes drafts by default while migration review is underway; build with `--build-arg INCLUDE_DRAFTS=false` for publication after pages are approved.
- No deployment was performed.

## Remaining work

1. Triage `hugo-link-validation.json`, separating genuine missing destinations from legacy sample HTML that should be fenced or escaped.
2. Repair or redirect genuine legacy paths within each version without cross-linking version-specific facts.
3. Complete technical review and set approved pages to `draft: false`.
4. Confirm the production hostname and override `baseURL` if needed.

