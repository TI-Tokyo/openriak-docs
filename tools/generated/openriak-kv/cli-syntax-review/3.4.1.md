# OpenRiak KV 3.4.1: syntax review

197 of 276 command topics need manual review.

Generated forms use the command tree, structured argument/option metadata and docs annotations. Supplied forms are retained for comparison. Whitespace and the riak-admin spelling are normalized; other differences are flagged conservatively, including differences that may be equivalent syntax.

Incomplete candidates are review material, not verified invocations. Pages retain supplied usage when requiredness, repetition, value arity or grouping is unknown. No source metadata is rewritten.

## erlang:riak_client:aae_fold

Page: `reference/commands/erlang/riak-client/aae-fold/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:erase_keys

Page: `reference/commands/erlang/riak-client/aae-fold/erase-keys/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({erase_keys, Bucket, KeyRange, SegmentFilter, ModifiedRange, count}).
riak_client:aae_fold({erase_keys, Bucket, KeyRange, SegmentFilter, ModifiedRange, local}).
riak_client:aae_fold({erase_keys, Bucket, KeyRange, SegmentFilter, ModifiedRange, {job, JobId}}).
riak_client:aae_fold({erase_keys, Bucket, KeyRange, SegmentFilter, ModifiedRange, count}, Client).
riak_client:aae_fold({erase_keys, Bucket, KeyRange, SegmentFilter, ModifiedRange, local}, Client).
riak_client:aae_fold({erase_keys, Bucket, KeyRange, SegmentFilter, ModifiedRange, {job, JobId}}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:fetch_clocks_nval

Page: `reference/commands/erlang/riak-client/aae-fold/fetch-clocks-nval/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({fetch_clocks_nval, NVal, Segments}).
riak_client:aae_fold({fetch_clocks_nval, NVal, Segments, ModifiedRange}).
riak_client:aae_fold({fetch_clocks_nval, NVal, Segments}, Client).
riak_client:aae_fold({fetch_clocks_nval, NVal, Segments, ModifiedRange}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:fetch_clocks_range

Page: `reference/commands/erlang/riak-client/aae-fold/fetch-clocks-range/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({fetch_clocks_range, Bucket, KeyRange, SegmentFilter, ModifiedRange}).
riak_client:aae_fold({fetch_clocks_range, Bucket, KeyRange, SegmentFilter, ModifiedRange}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:find_keys

Page: `reference/commands/erlang/riak-client/aae-fold/find-keys/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({find_keys, Bucket, KeyRange, ModifiedRange, Threshold}).
riak_client:aae_fold({find_keys, Bucket, KeyRange, ModifiedRange, Threshold}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:find_tombs

Page: `reference/commands/erlang/riak-client/aae-fold/find-tombs/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({find_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange}).
riak_client:aae_fold({find_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:list_buckets

Page: `reference/commands/erlang/riak-client/aae-fold/list-buckets/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({list_buckets, NVal}).
riak_client:aae_fold({list_buckets, NVal}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:merge_branch_nval

Page: `reference/commands/erlang/riak-client/aae-fold/merge-branch-nval/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({merge_branch_nval, NVal, BranchFilter}).
riak_client:aae_fold({merge_branch_nval, NVal, BranchFilter}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:merge_root_nval

Page: `reference/commands/erlang/riak-client/aae-fold/merge-root-nval/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({merge_root_nval, NVal}).
riak_client:aae_fold({merge_root_nval, NVal}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:merge_tree_range

Page: `reference/commands/erlang/riak-client/aae-fold/merge-tree-range/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({merge_tree_range, Bucket, KeyRange, TreeSize, SegmentFilter, ModifiedRange, HashMethod}).
riak_client:aae_fold({merge_tree_range, Bucket, KeyRange, TreeSize, SegmentFilter, ModifiedRange, HashMethod}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:object_stats

Page: `reference/commands/erlang/riak-client/aae-fold/object-stats/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({object_stats, Bucket, KeyRange, ModifiedRange}).
riak_client:aae_fold({object_stats, Bucket, KeyRange, ModifiedRange}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:reap_tombs

Page: `reference/commands/erlang/riak-client/aae-fold/reap-tombs/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({reap_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange, count}).
riak_client:aae_fold({reap_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange, local}).
riak_client:aae_fold({reap_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange, {job, JobId}}).
riak_client:aae_fold({reap_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange, count}, Client).
riak_client:aae_fold({reap_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange, local}, Client).
riak_client:aae_fold({reap_tombs, Bucket, KeyRange, SegmentFilter, ModifiedRange, {job, JobId}}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:repair_keys_range

Page: `reference/commands/erlang/riak-client/aae-fold/repair-keys-range/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({repair_keys_range, Bucket, KeyRange, ModifiedRange, Filter}).
riak_client:aae_fold({repair_keys_range, Bucket, KeyRange, ModifiedRange, Filter}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:aae_fold:repl_keys_range

Page: `reference/commands/erlang/riak-client/aae-fold/repl-keys-range/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:aae_fold({repl_keys_range, Bucket, KeyRange, ModifiedRange, QueueName}).
riak_client:aae_fold({repl_keys_range, Bucket, KeyRange, ModifiedRange, QueueName}, Client).
```

### Supplied syntax

```text
riak_client:aae_fold(Query).
riak_client:aae_fold(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:delete

Page: `reference/commands/erlang/riak-client/delete/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:delete(Bucket, Key, Client).
riak_client:delete(Bucket, Key, Options, Client).
riak_client:delete(Bucket, Key, RW, Client).
riak_client:delete(Bucket, Key, Options, Timeout, Client).
riak_client:delete(Bucket, Key, RW, Timeout, Client).
```

### Supplied syntax

```text
riak_client:delete(Bucket, Key, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:delete(Bucket, Key, Options, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:delete(Bucket, Key, RW, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:delete(Bucket, Key, Options, Timeout, {riak_client, [Node, _ClientId]} = THIS).
riak_client:delete(Bucket, Key, RW, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
```

## erlang:riak_client:delete_vclock

Page: `reference/commands/erlang/riak-client/delete-vclock/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:delete_vclock(Bucket, Key, VClock, Client).
riak_client:delete_vclock(Bucket, Key, VClock, Options, Client).
riak_client:delete_vclock(Bucket, Key, VClock, RW, Client).
riak_client:delete_vclock(Bucket, Key, VClock, Options, Timeout, Client).
riak_client:delete_vclock(Bucket, Key, VClock, RW, Timeout, Client).
```

### Supplied syntax

```text
riak_client:delete_vclock(Bucket, Key, VClock, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:delete_vclock(Bucket, Key, VClock, Options, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:delete_vclock(Bucket, Key, VClock, RW, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:delete_vclock(Bucket, Key, VClock, Options, Timeout, {riak_client, [Node, _ClientId]} = THIS).
riak_client:delete_vclock(Bucket, Key, VClock, RW, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
```

## erlang:riak_client:fetch

Page: `reference/commands/erlang/riak-client/fetch/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:fetch(QueueName, Client).
```

### Supplied syntax

```text
riak_client:fetch(QueueName, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:filter_buckets

Page: `reference/commands/erlang/riak-client/filter-buckets/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:filter_buckets(Fun, Client).
```

### Supplied syntax

```text
riak_client:filter_buckets(Fun, {riak_client, [_Node, _ClientId]} = THIS).
```

## erlang:riak_client:filter_keys

Page: `reference/commands/erlang/riak-client/filter-keys/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:filter_keys(Bucket, Fun, Client).
riak_client:filter_keys(Bucket, Fun, Timeout, Client).
```

### Supplied syntax

```text
riak_client:filter_keys(Bucket, Fun, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:filter_keys(Bucket, Fun, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
```

## erlang:riak_client:get

Page: `reference/commands/erlang/riak-client/get/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:get(Bucket, Key, Client).
riak_client:get(Bucket, Key, Options, Client).
riak_client:get(Bucket, Key, R, Client).
riak_client:get(Bucket, Key, R, Timeout, Client).
```

### Supplied syntax

```text
riak_client:get(Bucket, Key, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:get(Bucket, Key, Options, {riak_client, [Node, _ClientId]} = THIS).
riak_client:get(Bucket, Key, R, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:get(Bucket, Key, R, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
```

## erlang:riak_client:get_bucket

Page: `reference/commands/erlang/riak-client/get-bucket/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:get_bucket(Bucket, Client).
```

### Supplied syntax

```text
riak_client:get_bucket(BucketName, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:get_client_id

Page: `reference/commands/erlang/riak-client/get-client-id/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:get_client_id(Client).
```

### Supplied syntax

```text
riak_client:get_client_id({riak_client, [_Node, ClientId]}).
```

## erlang:riak_client:get_index

Page: `reference/commands/erlang/riak-client/get-index/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:get_index(Bucket, Query, Client).
riak_client:get_index(Bucket, Query, Opts, Client).
```

### Supplied syntax

```text
riak_client:get_index(Bucket, Query, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:get_index(Bucket, Query, Opts, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:get_stats

Page: `reference/commands/erlang/riak-client/get-stats/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:get_stats(local, Client).
riak_client:get_stats(global, Client).
```

### Supplied syntax

```text
riak_client:get_stats(local, {riak_client, [Node, _ClientId]}).
riak_client:get_stats(global, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:hotbackup

Page: `reference/commands/erlang/riak-client/hotbackup/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:hotbackup(BackupPath, DefaultNVal, PlanNVal, Client).
```

### Supplied syntax

```text
riak_client:hotbackup(BackupPath, DefaultNVal, PlanNVal, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:list_buckets

Page: `reference/commands/erlang/riak-client/list-buckets/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:list_buckets(Client).
riak_client:list_buckets(undefined, Client).
riak_client:list_buckets(Timeout, Client).
riak_client:list_buckets(Filter, Timeout, Client).
riak_client:list_buckets(Filter, Timeout, Type, Client).
```

### Supplied syntax

```text
riak_client:list_buckets({riak_client, [_Node, _ClientId]} = THIS).
riak_client:list_buckets(undefined, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:list_buckets(Timeout, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:list_buckets(Filter, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:list_buckets(Filter, Timeout, Type, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:list_keys

Page: `reference/commands/erlang/riak-client/list-keys/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:list_keys(Bucket, Client).
riak_client:list_keys(Bucket, Timeout, Client).
riak_client:list_keys(Bucket, Filter, Timeout, Client).
```

### Supplied syntax

```text
riak_client:list_keys(Bucket, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:list_keys(Bucket, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:list_keys(Bucket, Filter, Timeout0, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:push

Page: `reference/commands/erlang/riak-client/push/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:push(RObjMaybeBin, IsDeleted, Opts, Client).
```

### Supplied syntax

```text
riak_client:push(RObjMaybeBin, IsDeleted, Opts, RiakClient).
```

## erlang:riak_client:put

Page: `reference/commands/erlang/riak-client/put/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:put(RObj, Client).
riak_client:put(RObj, Options, Client).
riak_client:put(RObj, W, Client).
riak_client:put(RObj, W, DW, Client).
riak_client:put(RObj, W, DW, Timeout, Client).
riak_client:put(RObj, W, DW, Timeout, Options, Client).
```

### Supplied syntax

```text
riak_client:put(RObj, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:put(RObj, Options, {riak_client, [Node, _ClientId]} = THIS).
riak_client:put(RObj, W, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:put(RObj, W, DW, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:put(RObj, W, DW, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:put(RObj, W, DW, Timeout, Options, {riak_client, [_Node, _ClientId]} = THIS).
```

## erlang:riak_client:query

Page: `reference/commands/erlang/riak-client/query/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:query(Query, Client).
```

### Supplied syntax

```text
riak_client:query(Query, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:query_result_request

Page: `reference/commands/erlang/riak-client/query-result-request/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:query_result_request(ReqMap, Client).
```

### Supplied syntax

```text
riak_client:query_result_request(ReqMap, _Client).
```

## erlang:riak_client:reap

Page: `reference/commands/erlang/riak-client/reap/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:reap(Bucket, Key, Client).
riak_client:reap(Bucket, Key, TombClock, Client).
```

### Supplied syntax

```text
riak_client:reap(Bucket, Key, Client).
riak_client:reap(Bucket, Key, TombClock, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:reload_all

Page: `reference/commands/erlang/riak-client/reload-all/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:reload_all(Module, Client).
```

### Supplied syntax

```text
riak_client:reload_all(Module, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:remove_from_cluster

Page: `reference/commands/erlang/riak-client/remove-from-cluster/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:remove_from_cluster(ExitingNode, Client).
```

### Supplied syntax

```text
riak_client:remove_from_cluster(ExitingNode, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:reset_bucket

Page: `reference/commands/erlang/riak-client/reset-bucket/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:reset_bucket(Bucket, Client).
```

### Supplied syntax

```text
riak_client:reset_bucket(BucketName, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:set_bucket

Page: `reference/commands/erlang/riak-client/set-bucket/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:set_bucket(Bucket, BucketProps, Client).
```

### Supplied syntax

```text
riak_client:set_bucket(BucketName, BucketProps, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:stream_get_index

Page: `reference/commands/erlang/riak-client/stream-get-index/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:stream_get_index(Bucket, Query, Client).
riak_client:stream_get_index(Bucket, Query, Opts, Client).
```

### Supplied syntax

```text
riak_client:stream_get_index(Bucket, Query, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_get_index(Bucket, Query, Opts, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:stream_list_buckets

Page: `reference/commands/erlang/riak-client/stream-list-buckets/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:stream_list_buckets(Client).
riak_client:stream_list_buckets(undefined, Client).
riak_client:stream_list_buckets(Timeout, Client).
riak_client:stream_list_buckets(Filter, Client).
riak_client:stream_list_buckets(Filter, Timeout, Client).
riak_client:stream_list_buckets(Filter, Timeout, Recipient, Client).
riak_client:stream_list_buckets(Filter, Timeout, Type, Client).
riak_client:stream_list_buckets(Filter, Timeout, Recipient, Type, Client).
```

### Supplied syntax

```text
riak_client:stream_list_buckets({riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_buckets(undefined, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_buckets(Timeout, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_buckets(Filter, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_buckets(Filter, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_buckets(Filter, Timeout, Client, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_buckets(Filter, Timeout, Type, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_buckets(Filter, Timeout, Client, Type, {riak_client, [Node, _ClientId]}).
```

## erlang:riak_client:stream_list_keys

Page: `reference/commands/erlang/riak-client/stream-list-keys/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak_client:stream_list_keys(Bucket, Client).
riak_client:stream_list_keys(Bucket, undefined, Client).
riak_client:stream_list_keys(Bucket, Timeout, Client).
riak_client:stream_list_keys(Input, Timeout, Recipient, Client).
```

### Supplied syntax

```text
riak_client:stream_list_keys(Bucket, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_keys(Bucket, undefined, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_keys(Bucket, Timeout, {riak_client, [_Node, _ClientId]} = THIS).
riak_client:stream_list_keys(Input, Timeout, Client, {riak_client, [Node, _ClientId]}).
```

## shell:cuttlefish

Page: `reference/commands/helpers/cuttlefish/`

Displayed: known_path_only

- This parent also has argument or option declarations; their position relative to subcommands needs review.
- No supplied usage is available for comparison.

### Generated candidates

```text
cuttlefish { describe | effective | generate | help }
```

### Supplied syntax

```text
(not supplied)
```

## shell:cuttlefish describe

Page: `reference/commands/helpers/cuttlefish/describe/`

Displayed: known_path_only

- Whether --advanced_conf_file takes a value is not recorded.
- Whether --conf_file takes a value is not recorded.
- Whether --dest_dir takes a value is not recorded.
- Whether --dest_file takes a value is not recorded.
- Whether --etc_dir takes a value is not recorded.
- Whether --log_level takes a value is not recorded.
- Whether --max_history takes a value is not recorded.
- Whether --schema_dir takes a value is not recorded.
- Whether --schema_file takes a value is not recorded.
- No supplied usage is available for comparison.

### Generated candidates

```text
cuttlefish describe <variable> [--advanced_conf_file|-a <value?>] [--allow_extra|-x] [--conf_file|-c <value?> ...] [--dest_dir|-d <value?>] [--dest_file|-f <value?>] [--etc_dir|-e <value?>] [--help|-h] [--log_level|-l <value?>] [--max_history|-m <value?>] [--print|-p] [--schema_dir|-s <value?>] [--schema_file|-i <value?> ...] [--silent|-t]
```

### Supplied syntax

```text
(not supplied)
```

## shell:cuttlefish effective

Page: `reference/commands/helpers/cuttlefish/effective/`

Displayed: known_path_only

- Whether --advanced_conf_file takes a value is not recorded.
- Whether --conf_file takes a value is not recorded.
- Whether --dest_dir takes a value is not recorded.
- Whether --dest_file takes a value is not recorded.
- Whether --etc_dir takes a value is not recorded.
- Whether --log_level takes a value is not recorded.
- Whether --max_history takes a value is not recorded.
- Whether --schema_dir takes a value is not recorded.
- Whether --schema_file takes a value is not recorded.
- No supplied usage is available for comparison.

### Generated candidates

```text
cuttlefish effective [--advanced_conf_file|-a <value?>] [--allow_extra|-x] [--conf_file|-c <value?> ...] [--dest_dir|-d <value?>] [--dest_file|-f <value?>] [--etc_dir|-e <value?>] [--help|-h] [--log_level|-l <value?>] [--max_history|-m <value?>] [--print|-p] [--schema_dir|-s <value?>] [--schema_file|-i <value?> ...] [--silent|-t]
```

### Supplied syntax

```text
(not supplied)
```

## shell:cuttlefish generate

Page: `reference/commands/helpers/cuttlefish/generate/`

Displayed: known_path_only

- Whether --advanced_conf_file takes a value is not recorded.
- Whether --conf_file takes a value is not recorded.
- Whether --dest_dir takes a value is not recorded.
- Whether --dest_file takes a value is not recorded.
- Whether --etc_dir takes a value is not recorded.
- Whether --log_level takes a value is not recorded.
- Whether --max_history takes a value is not recorded.
- Whether --schema_dir takes a value is not recorded.
- Whether --schema_file takes a value is not recorded.
- No supplied usage is available for comparison.

### Generated candidates

```text
cuttlefish generate [--advanced_conf_file|-a <value?>] [--allow_extra|-x] [--conf_file|-c <value?> ...] [--dest_dir|-d <value?>] [--dest_file|-f <value?>] [--etc_dir|-e <value?>] [--help|-h] [--log_level|-l <value?>] [--max_history|-m <value?>] [--print|-p] [--schema_dir|-s <value?>] [--schema_file|-i <value?> ...] [--silent|-t]
```

### Supplied syntax

```text
(not supplied)
```

## shell:cuttlefish help

Page: `reference/commands/helpers/cuttlefish/help/`

Displayed: known_path_only

- Whether --advanced_conf_file takes a value is not recorded.
- Whether --conf_file takes a value is not recorded.
- Whether --dest_dir takes a value is not recorded.
- Whether --dest_file takes a value is not recorded.
- Whether --etc_dir takes a value is not recorded.
- Whether --log_level takes a value is not recorded.
- Whether --max_history takes a value is not recorded.
- Whether --schema_dir takes a value is not recorded.
- Whether --schema_file takes a value is not recorded.
- No supplied usage is available for comparison.

### Generated candidates

```text
cuttlefish help [--advanced_conf_file|-a <value?>] [--allow_extra|-x] [--conf_file|-c <value?> ...] [--dest_dir|-d <value?>] [--dest_file|-f <value?>] [--etc_dir|-e <value?>] [--help|-h] [--log_level|-l <value?>] [--max_history|-m <value?>] [--print|-p] [--schema_dir|-s <value?>] [--schema_file|-i <value?> ...] [--silent|-t]
```

### Supplied syntax

```text
(not supplied)
```

## shell:install_upgrade.escript

Page: `reference/commands/helpers/install-upgrade.escript/`

Displayed: known_path_only

- This parent also has argument or option declarations; their position relative to subcommands needs review.
- No supplied usage is available for comparison.

### Generated candidates

```text
install_upgrade.escript { downgrade | install | uninstall | unpack | upgrade | versions }
```

### Supplied syntax

```text
(not supplied)
```

## shell:install_upgrade.escript downgrade

Page: `reference/commands/helpers/install-upgrade.escript/downgrade/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
install_upgrade.escript downgrade <DistInfoStr> <Version> [--no-permanent]
```

### Supplied syntax

```text
(not supplied)
```

## shell:install_upgrade.escript install

Page: `reference/commands/helpers/install-upgrade.escript/install/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
install_upgrade.escript install <DistInfoStr> <Version> [--no-permanent]
```

### Supplied syntax

```text
(not supplied)
```

## shell:install_upgrade.escript uninstall

Page: `reference/commands/helpers/install-upgrade.escript/uninstall/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
install_upgrade.escript uninstall <DistInfoStr> <Version> [--no-permanent]
```

### Supplied syntax

```text
(not supplied)
```

## shell:install_upgrade.escript unpack

Page: `reference/commands/helpers/install-upgrade.escript/unpack/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
install_upgrade.escript unpack <DistInfoStr> <Version> [--no-permanent]
```

### Supplied syntax

```text
(not supplied)
```

## shell:install_upgrade.escript upgrade

Page: `reference/commands/helpers/install-upgrade.escript/upgrade/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
install_upgrade.escript upgrade <DistInfoStr> <Version> [--no-permanent]
```

### Supplied syntax

```text
(not supplied)
```

## shell:install_upgrade.escript versions

Page: `reference/commands/helpers/install-upgrade.escript/versions/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
install_upgrade.escript versions <DistInfoStr> [--no-permanent]
```

### Supplied syntax

```text
(not supplied)
```

## shell:nodetool

Page: `reference/commands/helpers/nodetool/`

Displayed: supplied_usage

- This parent also has argument or option declarations; their position relative to subcommands needs review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied alternatives absent from the discovered subcommands: ping, reboot, restart, rpcterms, stop.

### Generated candidates

```text
nodetool { eval | rpc }
```

### Supplied syntax

```text
Usage: nodetool {ping|stop|restart|reboot|rpc|rpcterms|eval [Terms]} [RPC]
```

## shell:nodetool eval

Page: `reference/commands/helpers/nodetool/eval/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: ping, stop, restart, reboot, rpc, rpcterms, RPC. Check for missing arguments, options or constraints.

### Generated candidates

```text
nodetool eval <Terms> [-name <TargetName>] [-setcookie <Cookie>] [-sname <TargetName>] [-start_epmd <StartEpmd>]
```

### Supplied syntax

```text
Usage: nodetool {ping|stop|restart|reboot|rpc|rpcterms|eval [Terms]} [RPC]
```

## shell:nodetool rpc

Page: `reference/commands/helpers/nodetool/rpc/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: ping, stop, restart, reboot, rpcterms, eval, Terms. Check for missing arguments, options or constraints.

### Generated candidates

```text
nodetool rpc <Module> <Function> [<Arguments> ...] [-name <TargetName>] [-setcookie <Cookie>] [-sname <TargetName>] [-start_epmd <StartEpmd>]
```

### Supplied syntax

```text
Usage: nodetool {ping|stop|restart|reboot|rpc|rpcterms|eval [Terms]} [RPC]
```

## shell:riak

Page: `reference/commands/riak/`

Displayed: generated

- Options scraped from this parent's usage may belong to subcommands and are omitted from the parent form: --no-permanent.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak { admin | attach | chkconfig | console | console_boot | console_clean | daemon | daemon_attach | daemon_boot | debug | downgrade | escript | eval | foreground | help | install | pid | ping | reboot | remote_console | repl | restart | rpc | start | status | stop | uninstall | unpack | upgrade | versions }
```

### Supplied syntax

```text
Usage: riak rpc [Mod [Fun [Args]]]]
Usage: riak eval [Exprs]
Usage: riak foreground
Usage: riak console
Usage: riak remote
Usage: riak unpack [VERSION]
Usage: riak install [VERSION]
Usage: riak uninstall [VERSION]
Usage: riak upgrade [VERSION]
Usage: riak downgrade [VERSION]
Usage: riak status
Usage: riak [COMMAND] [ARGS]
```

## shell:riak admin

Page: `reference/commands/riak/admin/`

Displayed: generated

- Options scraped from this parent's usage may belong to subcommands and are omitted from the parent form: -interval, -sort, -lines.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Discovered subcommands missing from the supplied alternatives: remove.
- Supplied alternatives absent from the discovered subcommands: diag.

### Generated candidates

```text
riak admin { aae-status | backup | bucket-type | cluster | cluster-info | describe | down | downgrade-objects | ensemble-status | erl-reload | force-remove | handoff | join | leave | member-status | node | reformat-indexes | reip | reip_manual | remove | repair-2i | restore | ring-status | ringready | security | services | set | show | stat | status | test | tictacaae | top | transfer-limit | transfers | vnode-status | wait-for-service }
```

### Supplied syntax

```text
Usage: riak admin { cluster | join | leave | backup | restore | test |
                    reip | reip_manual | erl-reload | wait-for-service |
                    ringready | transfers | force-remove | down |
                    cluster-info | member-status | ring-status | vnode-status |
                    tictacaae <subcommand> | node <subcommand> |
                    aae-status | diag | stat | status | transfer-limit | reformat-indexes |
                    top [-interval N] [-sort reductions|memory|msg_q] [-lines N] |
                    downgrade-objects | security | bucket-type | repair-2i |
                    services | ensemble-status | handoff | set |
                    show | describe }
```

## shell:riak admin backup

Page: `reference/commands/riak/admin/backup/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: all. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin backup <node> <cookie> <filename> <scope>
```

### Supplied syntax

```text
Usage: riak admin backup <node> <cookie> <filename> [node|all]
```

## shell:riak admin bucket-type

Page: `reference/commands/riak/admin/bucket-type/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin bucket-type { activate | create | list | status | update }
```

### Supplied syntax

```text
Usage: riak admin bucket-type <command>
```

## shell:riak admin bucket-type create

Page: `reference/commands/riak/admin/bucket-type/create/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: props. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin bucket-type create <type> [<properties>]
```

### Supplied syntax

```text
Usage: riak admin bucket-type create <type> ['{"props": { ... }}']
```

## shell:riak admin bucket-type update

Page: `reference/commands/riak/admin/bucket-type/update/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: props. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin bucket-type update <type> <properties>
```

### Supplied syntax

```text
Usage: riak admin bucket-type update <type> '{"props": { ... }}'
```

## shell:riak admin cluster

Page: `reference/commands/riak/admin/cluster/`

Displayed: generated

- Options scraped from this parent's usage may belong to subcommands and are omitted from the parent form: --node.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin cluster { clear | commit | force-remove | force-replace | join | leave | location | partition | partition-count | partitions | plan | replace | resize-ring | status }
```

### Supplied syntax

```text
Usage: riak-admin cluster <sub-command>
```

## shell:riak admin cluster-info

Page: `reference/commands/riak/admin/cluster-info/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: local. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin cluster-info <output_file> [<node> ...]
```

### Supplied syntax

```text
Usage: riak admin cluster-info <output_file> ['local' | <node> ['local' | <node>] [...]]
Usage: riak admin cluster_info <output_file> ['local' | <node> ['local' | <node>] [...]]
```

## shell:riak admin cluster clear

Page: `reference/commands/riak/admin/cluster/clear/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin cluster clear
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin cluster commit

Page: `reference/commands/riak/admin/cluster/commit/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin cluster commit
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin cluster location

Page: `reference/commands/riak/admin/cluster/location/`

Displayed: supplied_usage

- Recorded variants have different parameter declarations; their applicability needs review.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: node. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin cluster location <new_location> [--format {csv|human|json}] [--node|-n <value?>]
riak admin cluster location {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin cluster location <new_location> [--node node]
```

## shell:riak admin cluster partition

Page: `reference/commands/riak/admin/cluster/partition/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin cluster partition [<id>] [<index>] [--format {csv|human|json}]
riak admin cluster partition {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin cluster partition id=0
```

## shell:riak admin cluster partition-count

Page: `reference/commands/riak/admin/cluster/partition-count/`

Displayed: supplied_usage

- Recorded variants have different parameter declarations; their applicability needs review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: sub-command. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin cluster partition-count [--format {csv|human|json}] [--node|-n <<node>>]
riak admin cluster partition-count {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin cluster <sub-command>
Usage: riak-admin cluster partition-count [--node node]
```

## shell:riak admin cluster partitions

Page: `reference/commands/riak/admin/cluster/partitions/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin cluster partitions [--format {csv|human|json}] [--node|-n <<node>>]
riak admin cluster partitions {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin cluster partitions [--node node]
```

## shell:riak admin cluster plan

Page: `reference/commands/riak/admin/cluster/plan/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin cluster plan
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin cluster resize-ring

Page: `reference/commands/riak/admin/cluster/resize-ring/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin cluster resize-ring { abort }
```

### Supplied syntax

```text
Usage: riak admin cluster resize-ring <new-ring-size>
```

## shell:riak admin cluster resize-ring abort

Page: `reference/commands/riak/admin/cluster/resize-ring/abort/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin cluster resize-ring abort
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin cluster status

Page: `reference/commands/riak/admin/cluster/status/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin cluster status [--format {csv|human|json}]
riak admin cluster status {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin cluster status
```

## shell:riak admin describe

Page: `reference/commands/riak/admin/describe/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin describe <variable> ... [--format {csv|human|json}]
riak admin describe {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin describe <variable> ...
```

## shell:riak admin downgrade-objects

Page: `reference/commands/riak/admin/downgrade-objects/`

Displayed: supplied_usage

- Recorded variants have different parameter declarations; their applicability needs review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin downgrade-objects <kill-handoffs> [<concurrency>]
riak admin downgrade-objects false <kill-handoffs> [<concurrency>]
riak admin downgrade-objects true <kill-handoffs> [<concurrency>]
```

### Supplied syntax

```text
Usage: riak admin downgrade-objects <kill-handoffs> [<concurrency>]
Usage: riak admin downgrade_objects <kill-handoffs> [<concurrency>]
```

## shell:riak admin ensemble-status

Page: `reference/commands/riak/admin/ensemble-status/`

Displayed: supplied_usage

- Recorded variants have different parameter declarations; their applicability needs review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin ensemble-status [<ensemble>]
riak admin ensemble-status root [<ensemble>]
```

### Supplied syntax

```text
Usage: riak admin ensemble-status [<ensemble>]
Usage: riak admin ensemble_status [<ensemble>]
```

## shell:riak admin erl-reload

Page: `reference/commands/riak/admin/erl-reload/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin erl-reload
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin force-remove

Page: `reference/commands/riak/admin/force-remove/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin force-remove <node> [-f]
```

### Supplied syntax

```text
Usage: riak admin force-remove -f <node>
```

## shell:riak admin handoff

Page: `reference/commands/riak/admin/handoff/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin handoff { config | details | disable | enable | summary }
```

### Supplied syntax

```text
Usage: riak-admin handoff <sub-command>
```

## shell:riak admin handoff config

Page: `reference/commands/riak/admin/handoff/config/`

Displayed: supplied_usage

- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin handoff config [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff config {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin handoff config
```

## shell:riak admin handoff details

Page: `reference/commands/riak/admin/handoff/details/`

Displayed: supplied_usage

- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: node. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin handoff details [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff details {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin handoff details [--node <node>|--all]
```

## shell:riak admin handoff disable

Page: `reference/commands/riak/admin/handoff/disable/`

Displayed: supplied_usage

- Recorded variants have different parameter declarations; their applicability needs review.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: enable, node. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin handoff disable <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff disable both <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff disable inbound <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff disable outbound <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff disable {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin handoff <enable|disable> <inbound|outbound|both> [-n <node>|--all]
```

## shell:riak admin handoff enable

Page: `reference/commands/riak/admin/handoff/enable/`

Displayed: supplied_usage

- Recorded variants have different parameter declarations; their applicability needs review.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: disable, node. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin handoff enable <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff enable both <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff enable inbound <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff enable outbound <direction> [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin handoff enable {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin handoff <enable|disable> <inbound|outbound|both> [-n <node>|--all]
```

## shell:riak admin handoff summary

Page: `reference/commands/riak/admin/handoff/summary/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin handoff summary [--format {csv|human|json}]
riak admin handoff summary {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin handoff summary
```

## shell:riak admin join

Page: `reference/commands/riak/admin/join/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin join <node> [-f]
```

### Supplied syntax

```text
Usage: riak admin join -f <node>
```

## shell:riak admin leave

Page: `reference/commands/riak/admin/leave/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin leave [-f]
```

### Supplied syntax

```text
Usage: riak admin leave -f
```

## shell:riak admin node

Page: `reference/commands/riak/admin/node/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin node { repair }
```

### Supplied syntax

```text
Usage: riak admin node repair { start | status | stop REASON } [OPTIONS]
```

## shell:riak admin node repair

Page: `reference/commands/riak/admin/node/repair/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin node repair { start | status | stop }
```

### Supplied syntax

```text
Usage: riak admin node repair { start | status | stop REASON } [OPTIONS]
```

## shell:riak admin node repair start

Page: `reference/commands/riak/admin/node/repair/start/`

Displayed: supplied_usage

- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin node repair start [--format {csv|human|json}] [--node|-n <value?>]
riak admin node repair start {--help|-h}
```

### Supplied syntax

```text
Usage: riak admin node repair start [-n|--node NODE]
```

## shell:riak admin node repair status

Page: `reference/commands/riak/admin/node/repair/status/`

Displayed: supplied_usage

- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: all. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin node repair status [--format {csv|human|json}] [--node|-n <value?>] [-f {table|json}]
riak admin node repair status {--help|-h}
```

### Supplied syntax

```text
Usage: riak admin node repair status [-n|--node NODE|all] [-f|--format table|json]
```

## shell:riak admin node repair stop

Page: `reference/commands/riak/admin/node/repair/stop/`

Displayed: supplied_usage

- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: all. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin node repair stop <REASON> [--format {csv|human|json}] [--node|-n <value?>]
riak admin node repair stop {--help|-h}
```

### Supplied syntax

```text
Usage: riak admin node repair stop [-n|--node NODE|all] REASON
```

## shell:riak admin reformat-indexes

Page: `reference/commands/riak/admin/reformat-indexes/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin reformat-indexes [<concurrency>] [<batch size>] [--downgrade]
```

### Supplied syntax

```text
Usage: riak admin reformat-indexes
Usage: riak admin reformat_indexes
```

## shell:riak admin reip

Page: `reference/commands/riak/admin/reip/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: is, not, currently, supported, in. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin reip <old_nodename> <new_nodename>
```

### Supplied syntax

```text
Usage riak admin reip <old_nodename> <new_nodename>
Usage riak admin reip is not currently supported in Riak 3.0
```

## shell:riak admin remove

Page: `reference/commands/riak/admin/remove/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin remove
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin repair-2i

Page: `reference/commands/riak/admin/repair-2i/`

Displayed: supplied_usage

- Options scraped from this parent's usage may belong to subcommands and are omitted from the parent form: --speed.
- This parent also has argument or option declarations; their position relative to subcommands needs review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin repair-2i { kill | status }
```

### Supplied syntax

```text
Usage: riak-admin repair-2i [--speed [1-100]] <Idx> ...
```

## shell:riak admin repair-2i kill

Page: `reference/commands/riak/admin/repair-2i/kill/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: --speed, -100, Idx. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin repair-2i kill
```

### Supplied syntax

```text
Usage: riak-admin repair-2i [--speed [1-100]] <Idx> ...
```

## shell:riak admin repair-2i status

Page: `reference/commands/riak/admin/repair-2i/status/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: --speed, -100, Idx. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin repair-2i status
```

### Supplied syntax

```text
Usage: riak-admin repair-2i [--speed [1-100]] <Idx> ...
```

## shell:riak admin search

Page: `reference/commands/riak/admin/search/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin search
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin security

Page: `reference/commands/riak/admin/security/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin security { add-group | add-source | add-user | alter-group | alter-user | ciphers | del-group | del-source | del-user | disable | enable | grant | print-grants | print-group | print-groups | print-sources | print-user | print-users | revoke | status }
```

### Supplied syntax

```text
Usage: riak admin security <command>
```

## shell:riak admin security add-group

Page: `reference/commands/riak/admin/security/add-group/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin security add-group <group> [<option=value> ...]
```

### Supplied syntax

```text
Usage: riak admin security add-group <group> [<option>=<value> [...]]
```

## shell:riak admin security add-source

Page: `reference/commands/riak/admin/security/add-source/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: all. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin security add-source <users> <CIDR> <source> [<option=value> ...]
```

### Supplied syntax

```text
Usage: riak admin security add-source all|<users> <CIDR> <source> [<option>=<value> [...]]
```

## shell:riak admin security add-user

Page: `reference/commands/riak/admin/security/add-user/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin security add-user <user> [<option=value> ...]
```

### Supplied syntax

```text
Usage: riak admin security add-user <user> [<option>=<value> [...]]
```

## shell:riak admin security alter-group

Page: `reference/commands/riak/admin/security/alter-group/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin security alter-group <group> [<option=value> ...]
```

### Supplied syntax

```text
Usage: riak admin security alter-group <group> <option> [<option>=<value> [...]]
```

## shell:riak admin security alter-user

Page: `reference/commands/riak/admin/security/alter-user/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin security alter-user <user> [<option=value> ...]
```

### Supplied syntax

```text
Usage: riak admin security alter-user <user> <option> [<option>=<value> [...]]
```

## shell:riak admin security ciphers

Page: `reference/commands/riak/admin/security/ciphers/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin security ciphers
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin security del-group

Page: `reference/commands/riak/admin/security/del-group/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: user. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin security del-group <group>
```

### Supplied syntax

```text
Usage: riak admin security del-group <user>
```

## shell:riak admin security del-source

Page: `reference/commands/riak/admin/security/del-source/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: all. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin security del-source <users> <CIDR>
```

### Supplied syntax

```text
Usage: riak admin security del-source all|<users> <CIDR>
```

## shell:riak admin security disable

Page: `reference/commands/riak/admin/security/disable/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin security disable
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin security enable

Page: `reference/commands/riak/admin/security/enable/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin security enable
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin security grant

Page: `reference/commands/riak/admin/security/grant/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: on, type, bucket, any, to. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin security grant <permissions> <scope> <users>
```

### Supplied syntax

```text
Usage: riak admin security grant <permissions> on any|<type> [bucket] to <users>
Usage: grant <permissions> on (<type> [bucket]|any) to <users>
```

## shell:riak admin security print-groups

Page: `reference/commands/riak/admin/security/print-groups/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin security print-groups
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin security print-sources

Page: `reference/commands/riak/admin/security/print-sources/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin security print-sources
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin security print-users

Page: `reference/commands/riak/admin/security/print-users/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin security print-users
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin security revoke

Page: `reference/commands/riak/admin/security/revoke/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: on, type, bucket, from, any. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin security revoke <permissions> <scope> <users>
```

### Supplied syntax

```text
Usage: riak admin security revoke <permissions> on any|<type> [bucket] from <users>
Usage: revoke <permissions> on <type> [bucket] from <users>
```

## shell:riak admin security status

Page: `reference/commands/riak/admin/security/status/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin security status
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin set

Page: `reference/commands/riak/admin/set/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: node. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin set <variable=value> ... [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin set {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin set <variable>=<value> ... [[--node | -n] <node> | --all]
```

## shell:riak admin show

Page: `reference/commands/riak/admin/show/`

Displayed: supplied_usage

- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: node. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin show <variable> ... [--all|-a] [--format {csv|human|json}] [--node|-n <value?>]
riak admin show {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin show <variable> ... [[--node | -n] <node> | --all]
```

## shell:riak admin stat

Page: `reference/commands/riak/admin/stat/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin stat { disable | enable | info | reset | show }
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin stat disable

Page: `reference/commands/riak/admin/stat/disable/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin stat disable <entry>
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin stat enable

Page: `reference/commands/riak/admin/stat/enable/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin stat enable <entry>
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin stat info

Page: `reference/commands/riak/admin/stat/info/`

Displayed: known_path_only

- Recorded variants have different parameter declarations; their applicability needs review.
- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin stat info <entry> [-cache ...] [-module ...] [-name ...] [-options ...] [-status ...] [-timestamp ...] [-type ...] [-value ...]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin stat reset

Page: `reference/commands/riak/admin/stat/reset/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin stat reset <entry>
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin stat show

Page: `reference/commands/riak/admin/stat/show/`

Displayed: supplied_usage

- Recorded variants have different parameter declarations; their applicability needs review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: type, status, dp. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin stat show <entry>
```

### Supplied syntax

```text
Usage: riak admin stat show <entry>[/type=<type>][/status=<status>][/<dp>[,<dp>]]
```

## shell:riak admin test

Page: `reference/commands/riak/admin/test/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin test
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin tictacaae

Page: `reference/commands/riak/admin/tictacaae/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin tictacaae { exchangetick | fold | maxresults | rangeboost | rebuild-now | rebuild-schedule | rebuild-soon | rebuildtick | storeheads | tokenbucket | treestatus }
```

### Supplied syntax

```text
Usage: riak-admin tictacaae { rebuild-schedule | storeheads | tokenbucket
                     | rebuildtick | exchangetick | maxresults | rangeboost
                     | rebuild-soon | rebuild-now | treestatus | fold
                     }
```

## shell:riak admin tictacaae exchangetick

Page: `reference/commands/riak/admin/tictacaae/exchangetick/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, show, env, var, VAR, on, NODE. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae exchangetick [<VALUE>] [--format {csv|human|json}] [--node|-n <value?>]
riak admin tictacaae exchangetick {--help|-h}
```

### Supplied syntax

```text
Usage: Set/show env var VAR on NODE:
```

## shell:riak admin tictacaae fold

Page: `reference/commands/riak/admin/tictacaae/fold/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Recorded variants have different parameter declarations; their applicability needs review.
- Whether --outfile takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: AAE, operations, dumping, results, in, format, to, a, file, specified, with. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae fold <operation> [<nval>] [<bucket>] [<key_range>] [<modified_range>] [<segments>] [<sibling_count>] [<object_size>] [<change_method>] [--format {csv|human|json}] [--outfile|-o <value?>]
riak admin tictacaae fold {--help|-h}
```

### Supplied syntax

```text
Usage: AAE fold operations, dumping results in JSON format to a file specified with '-o'.
```

## shell:riak admin tictacaae maxresults

Page: `reference/commands/riak/admin/tictacaae/maxresults/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, show, env, var, VAR, on, NODE. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae maxresults [<VALUE>] [--format {csv|human|json}] [--node|-n <value?>]
riak admin tictacaae maxresults {--help|-h}
```

### Supplied syntax

```text
Usage: Set/show env var VAR on NODE:
```

## shell:riak admin tictacaae rangeboost

Page: `reference/commands/riak/admin/tictacaae/rangeboost/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, show, env, var, VAR, on, NODE. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae rangeboost [<VALUE>] [--format {csv|human|json}] [--node|-n <value?>]
riak admin tictacaae rangeboost {--help|-h}
```

### Supplied syntax

```text
Usage: Set/show env var VAR on NODE:
```

## shell:riak admin tictacaae rebuild-now

Page: `reference/commands/riak/admin/tictacaae/rebuild-now/`

Displayed: supplied_usage

- Whether --node takes a value is not recorded.
- Whether --partition takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Send, a, rebuild, poke, for, PARTITION, on, NODE, default, is, all, partitions. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae rebuild-now [--format {csv|human|json}] [--node|-n <value?>] [--partition|-p <value?>]
riak admin tictacaae rebuild-now {--help|-h}
```

### Supplied syntax

```text
Usage: Send a rebuild poke for PARTITION on NODE (default is all partitions
```

## shell:riak admin tictacaae rebuild-schedule

Page: `reference/commands/riak/admin/tictacaae/rebuild-schedule/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Whether --partition takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, show, rebuild, schedule, on, an, AAE, controller, managing, PARTITION, NODE. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae rebuild-schedule [<RW>] [<RD>] [--format {csv|human|json}] [--node|-n <value?>] [--partition|-p <value?>]
riak admin tictacaae rebuild-schedule {--help|-h}
```

### Supplied syntax

```text
Usage: Set/show rebuild schedule on an AAE controller managing PARTITION on NODE:
```

## shell:riak admin tictacaae rebuild-soon

Page: `reference/commands/riak/admin/tictacaae/rebuild-soon/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Recorded variants have different parameter declarations; their applicability needs review.
- Whether --node takes a value is not recorded.
- Whether --partition takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, next, rebuild, time, to, now, sec, on, PARTITION, NODE, default, is. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae rebuild-soon <DELAY> [--format {csv|human|json}] [--node|-n <value?>] [--partition|-p <value?>]
riak admin tictacaae rebuild-soon {--help|-h}
```

### Supplied syntax

```text
Usage: Set next rebuild time to now + DELAY sec, on PARTITION on NODE (default is
```

## shell:riak admin tictacaae rebuildtick

Page: `reference/commands/riak/admin/tictacaae/rebuildtick/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, show, env, var, VAR, on, NODE. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae rebuildtick [<VALUE>] [--format {csv|human|json}] [--node|-n <value?>]
riak admin tictacaae rebuildtick {--help|-h}
```

### Supplied syntax

```text
Usage: Set/show env var VAR on NODE:
```

## shell:riak admin tictacaae storeheads

Page: `reference/commands/riak/admin/tictacaae/storeheads/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Whether --partition takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, show, flag, on, an, AAE, controller, managing, PARTITION, NODE. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae storeheads [<VALUE>] [--format {csv|human|json}] [--node|-n <value?>] [--partition|-p <value?>]
riak admin tictacaae storeheads {--help|-h}
```

### Supplied syntax

```text
Usage: Set/show storeheads flag on an AAE controller managing PARTITION on NODE:
```

## shell:riak admin tictacaae tokenbucket

Page: `reference/commands/riak/admin/tictacaae/tokenbucket/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Whether --node takes a value is not recorded.
- Whether --partition takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Set, show, flag, on, a, vnode, managing, PARTITION, NODE. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae tokenbucket [<VALUE>] [--format {csv|human|json}] [--node|-n <value?>] [--partition|-p <value?>]
riak admin tictacaae tokenbucket {--help|-h}
```

### Supplied syntax

```text
Usage: Set/show tokenbucket flag on a vnode managing PARTITION on NODE:
```

## shell:riak admin tictacaae treestatus

Page: `reference/commands/riak/admin/tictacaae/treestatus/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: Generate, the, tree, rebuild, report. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin tictacaae treestatus [--format {csv|human|json}] [--show|-s {empty|partial|built|rebuilding|building|all}]
riak admin tictacaae treestatus {--help|-h}
```

### Supplied syntax

```text
Usage: Generate the tree rebuild report:
```

## shell:riak admin top

Page: `reference/commands/riak/admin/top/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak admin top [-interval <N>] [-lines <N>] [-sort {reductions|memory|msg_q}]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak admin transfer-limit

Page: `reference/commands/riak/admin/transfer-limit/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak admin transfer-limit [<node>] [<limit>]
```

### Supplied syntax

```text
Usage: riak admin transfer-limit
Usage: riak admin transfer_limit
```

## shell:riak admin vnode-status

Page: `reference/commands/riak/admin/vnode-status/`

Displayed: supplied_usage

- The number and requiredness of unrestricted key=value arguments are not recorded.
- Recorded variants have different parameter declarations; their applicability needs review.
- Whether --node takes a value is not recorded.
- Whether --partition takes a value is not recorded.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: NODE, all, PARTITION. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak admin vnode-status [--format {csv|human|json}] [--node|-n <value?>] [--partition|-p <value?>]
riak admin vnode-status {--help|-h}
```

### Supplied syntax

```text
Usage: riak-admin vnode-status [-n|--node NODE|all] [-p|--partition PARTITION|all]
```

## shell:riak attach

Page: `reference/commands/riak/attach/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak attach
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak chkconfig

Page: `reference/commands/riak/chkconfig/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak chkconfig
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak console_boot

Page: `reference/commands/riak/console-boot/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak console_boot <bootfile>
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak console_clean

Page: `reference/commands/riak/console-clean/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak console_clean
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak daemon

Page: `reference/commands/riak/daemon/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak daemon
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak daemon_attach

Page: `reference/commands/riak/daemon-attach/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak daemon_attach
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak daemon_boot

Page: `reference/commands/riak/daemon-boot/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak daemon_boot <bootfile>
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak debug

Page: `reference/commands/riak/debug/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak debug [<FILENAME>] [--cfgs] [--extracmds] [--help] [--logs] [--patches] [--riakcmds] [--ssl-certs] [--syscmds] [--verbose] [--yzcmds] [-c] [-e] [-h] [-l] [-p] [-r] [-s] [-v] [-y]
```

### Supplied syntax

```text
Usage: riak-debug [-c] [-l] [-r] [-s] [-e] [-v] [FILENAME | -]
```

## shell:riak downgrade

Page: `reference/commands/riak/downgrade/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak downgrade <version> [--no-permanent]
```

### Supplied syntax

```text
Usage: riak downgrade {version}
Usage: riak downgrade [VERSION]
```

## shell:riak escript

Page: `reference/commands/riak/escript/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak escript <script> [<arguments> ...]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak eval

Page: `reference/commands/riak/eval/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak eval <Exprs>
```

### Supplied syntax

```text
Usage: riak eval [Exprs]
```

## shell:riak help

Page: `reference/commands/riak/help/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: COMMAND, ARGS, console, downgrade, VERSION, eval, Exprs, foreground, install, remote_console, rpc, Mod, Fun, Args, status, uninstall, unpack, upgrade. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak help [<topic>]
```

### Supplied syntax

```text
Usage: riak rpc [Mod [Fun [Args]]]]
Usage: riak eval [Exprs]
Usage: riak foreground
Usage: riak console
Usage: riak remote
Usage: riak unpack [VERSION]
Usage: riak install [VERSION]
Usage: riak uninstall [VERSION]
Usage: riak upgrade [VERSION]
Usage: riak downgrade [VERSION]
Usage: riak status
Usage: riak [COMMAND] [ARGS]
```

## shell:riak install

Page: `reference/commands/riak/install/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak install <version> [--no-permanent]
```

### Supplied syntax

```text
Usage: riak install {version}
Usage: riak install [VERSION]
```

## shell:riak pid

Page: `reference/commands/riak/pid/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak pid
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak ping

Page: `reference/commands/riak/ping/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak ping
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak reboot

Page: `reference/commands/riak/reboot/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak reboot
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl

Page: `reference/commands/riak/repl/`

Displayed: generated

- Options scraped from this parent's usage may belong to subcommands and are omitted from the parent form: -port, -block-provider-redirect.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak repl { add-block-provider-redirect | add-listener | add-nat-listener | add-site | cancel-fullsync | clustername | clusters | clusterstats | connect | connections | del-listener | del-site | delete-block-provider-redirect | disconnect | fullsync | modes | nat-map | pause-fullsync | proxy_get | realtime | resume-fullsync | show-block-provider-redirect | show-local-cluster-id | start-fullsync | status }
```

### Supplied syntax

```text
Usage riak repl $ACTION <nodename> <listen_ip> <port>
Usage riak repl $ACTION <nodename> <listen_ip> <port> <public_ip> <public_port>
Usage: riak repl $ACTION <ipaddr> <portnum> <sitename>
Usage: riak repl $ACTION <sitename>
Usage: riak repl $ACTION [clustername]
Usage: riak repl $ACTION
Usage: riak repl $ACTION  <host:port>
Usage: riak repl $ACTION {<host:port> | <clustername>}
Usage: riak repl $ACTION enable <clustername> |
                       disable <clustername> |
                       start [<clustername>] |
                       stop [<clustername>] |
                       cascades [ always | never ] | (realtime only)
                       max_fssource_node [<value>] | (fullsync only)
                       max_fssource_cluster [<value>] | (fullsync only)
                       max_fssink_node [<value>] (fullsync only)
Usage: riak repl $ACTION {enable|disable} <clustername>
Usage: riak repl $ACTION realtime start [clustername] |
                       realtime stop [clustername]
Usage: riak repl $ACTION show |
                       add <externalip>[:port] <internalip> |
                       del <externalip>[:port] <internalip>
Usage: riak repl $ACTION $SUB_CMD
Usage: riak repl $ACTION $SUB_CMD <externalip>[:port] <internalip>
Usage: riak repl <command>
```

## shell:riak repl add-block-provider-redirect

Page: `reference/commands/riak/repl/add-block-provider-redirect/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: show, block-provider-redirect. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl add-block-provider-redirect <from-cluster> <to-cluster>
```

### Supplied syntax

```text
Usage: riak repl add-block-provider-redirect show |
                       block-provider-redirect <from-cluster> <to-cluster>
```

## shell:riak repl cancel-fullsync

Page: `reference/commands/riak/repl/cancel-fullsync/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl cancel-fullsync
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl clustername

Page: `reference/commands/riak/repl/clustername/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak repl clustername [<clustername>]
```

### Supplied syntax

```text
Usage: riak repl clustername [clustername]
```

## shell:riak repl clusterstats

Page: `reference/commands/riak/repl/clusterstats/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl clusterstats [<connection>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl connect

Page: `reference/commands/riak/repl/connect/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: host, port, clustername. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl connect <destination>
```

### Supplied syntax

```text
Usage: riak repl connect  <host:port>
Usage: riak repl connect {<host:port> | <clustername>}
```

## shell:riak repl delete-block-provider-redirect

Page: `reference/commands/riak/repl/delete-block-provider-redirect/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: show. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl delete-block-provider-redirect <from-cluster>
```

### Supplied syntax

```text
Usage: riak repl delete-block-provider-redirect show |
                       delete-block-provider-redirect <from-cluster>
```

## shell:riak repl disconnect

Page: `reference/commands/riak/repl/disconnect/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: host, port, clustername. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl disconnect <destination>
```

### Supplied syntax

```text
Usage: riak repl disconnect  <host:port>
Usage: riak repl disconnect {<host:port> | <clustername>}
```

## shell:riak repl fullsync

Page: `reference/commands/riak/repl/fullsync/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak repl fullsync { disable | enable | max_fssink_node | max_fssource_cluster | max_fssource_node | start | stop }
```

### Supplied syntax

```text
Usage: riak repl fullsync enable <clustername> |
                       disable <clustername> |
                       start [<clustername>] |
                       stop [<clustername>] |
                       cascades [ always | never ] | (realtime only)
                       max_fssource_node [<value>] | (fullsync only)
                       max_fssource_cluster [<value>] | (fullsync only)
                       max_fssink_node [<value>] (fullsync only)
```

## shell:riak repl fullsync disable

Page: `reference/commands/riak/repl/fullsync/disable/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: enable. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl fullsync disable <clustername>
```

### Supplied syntax

```text
Usage: riak repl fullsync {enable|disable} <clustername>
```

## shell:riak repl fullsync enable

Page: `reference/commands/riak/repl/fullsync/enable/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: disable. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl fullsync enable <clustername>
```

### Supplied syntax

```text
Usage: riak repl fullsync {enable|disable} <clustername>
```

## shell:riak repl fullsync max_fssink_node

Page: `reference/commands/riak/repl/fullsync/max-fssink-node/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl fullsync max_fssink_node [<value>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl fullsync max_fssource_cluster

Page: `reference/commands/riak/repl/fullsync/max-fssource-cluster/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl fullsync max_fssource_cluster [<value>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl fullsync max_fssource_node

Page: `reference/commands/riak/repl/fullsync/max-fssource-node/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl fullsync max_fssource_node [<value>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl fullsync start

Page: `reference/commands/riak/repl/fullsync/start/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl fullsync start [<clustername>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl fullsync stop

Page: `reference/commands/riak/repl/fullsync/stop/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl fullsync stop [<clustername>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl modes

Page: `reference/commands/riak/repl/modes/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl modes
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl nat-map

Page: `reference/commands/riak/repl/nat-map/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak repl nat-map { add | del | show }
```

### Supplied syntax

```text
Usage: riak repl nat-map show |
                       add <externalip>[:port] <internalip> |
                       del <externalip>[:port] <internalip>
```

## shell:riak repl nat-map add

Page: `reference/commands/riak/repl/nat-map/add/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: port. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl nat-map add <externalip> <internalip>
```

### Supplied syntax

```text
Usage: riak repl nat-map add <externalip>[:port] <internalip>
```

## shell:riak repl nat-map del

Page: `reference/commands/riak/repl/nat-map/del/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: port. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl nat-map del <externalip> <internalip>
```

### Supplied syntax

```text
Usage: riak repl nat-map del <externalip>[:port] <internalip>
```

## shell:riak repl pause-fullsync

Page: `reference/commands/riak/repl/pause-fullsync/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl pause-fullsync
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl proxy_get

Page: `reference/commands/riak/repl/proxy-get/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak repl proxy_get { disable | enable }
```

### Supplied syntax

```text
Usage: riak repl proxy_get enable <clustername> |
                       disable <clustername> |
```

## shell:riak repl proxy_get disable

Page: `reference/commands/riak/repl/proxy-get/disable/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: enable. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl proxy_get disable <clustername>
```

### Supplied syntax

```text
Usage: riak repl proxy_get {enable|disable} <clustername>
```

## shell:riak repl proxy_get enable

Page: `reference/commands/riak/repl/proxy-get/enable/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: disable. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl proxy_get enable <clustername>
```

### Supplied syntax

```text
Usage: riak repl proxy_get {enable|disable} <clustername>
```

## shell:riak repl realtime

Page: `reference/commands/riak/repl/realtime/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak repl realtime { cascades | disable | enable | start | stop }
```

### Supplied syntax

```text
Usage: riak repl realtime enable <clustername> |
                       disable <clustername> |
                       start [<clustername>] |
                       stop [<clustername>] |
                       cascades [ always | never ] | (realtime only)
                       max_fssource_node [<value>] | (fullsync only)
                       max_fssource_cluster [<value>] | (fullsync only)
                       max_fssink_node [<value>] (fullsync only)
```

## shell:riak repl realtime cascades

Page: `reference/commands/riak/repl/realtime/cascades/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl realtime cascades [<policy>]
riak repl realtime cascades always [<policy>]
riak repl realtime cascades never [<policy>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl realtime disable

Page: `reference/commands/riak/repl/realtime/disable/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: enable. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl realtime disable <clustername>
```

### Supplied syntax

```text
Usage: riak repl realtime {enable|disable} <clustername>
```

## shell:riak repl realtime enable

Page: `reference/commands/riak/repl/realtime/enable/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: disable. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl realtime enable <clustername>
```

### Supplied syntax

```text
Usage: riak repl realtime {enable|disable} <clustername>
```

## shell:riak repl realtime start

Page: `reference/commands/riak/repl/realtime/start/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl realtime start [<clustername>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl realtime stop

Page: `reference/commands/riak/repl/realtime/stop/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl realtime stop [<clustername>]
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl resume-fullsync

Page: `reference/commands/riak/repl/resume-fullsync/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl resume-fullsync
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl show-block-provider-redirect

Page: `reference/commands/riak/repl/show-block-provider-redirect/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: show. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl show-block-provider-redirect <from-cluster>
```

### Supplied syntax

```text
Usage: riak repl show-block-provider-redirect show |
                       show-block-provider-redirect <from-cluster>
```

## shell:riak repl show-local-cluster-id

Page: `reference/commands/riak/repl/show-local-cluster-id/`

Displayed: supplied_usage

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).
- Supplied syntax contains terms absent from the structured form: show. Check for missing arguments, options or constraints.

### Generated candidates

```text
riak repl show-local-cluster-id
```

### Supplied syntax

```text
Usage: riak repl show-local-cluster-id show |
                       show-local-cluster-id
```

## shell:riak repl start-fullsync

Page: `reference/commands/riak/repl/start-fullsync/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl start-fullsync
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak repl status

Page: `reference/commands/riak/repl/status/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak repl status
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak restart

Page: `reference/commands/riak/restart/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak restart
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak rpc

Page: `reference/commands/riak/rpc/`

Displayed: generated

- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak rpc <Mod> <Fun> [<Args> ...]
```

### Supplied syntax

```text
Usage: riak rpc [Mod [Fun [Args]]]]
```

## shell:riak start

Page: `reference/commands/riak/start/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak start
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak stop

Page: `reference/commands/riak/stop/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak stop
```

### Supplied syntax

```text
(not supplied)
```

## shell:riak uninstall

Page: `reference/commands/riak/uninstall/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak uninstall <version>
```

### Supplied syntax

```text
Usage: riak uninstall {version}
Usage: riak uninstall [VERSION]
```

## shell:riak unpack

Page: `reference/commands/riak/unpack/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak unpack <version>
```

### Supplied syntax

```text
Usage: riak unpack {version}
Usage: riak unpack [VERSION]
```

## shell:riak upgrade

Page: `reference/commands/riak/upgrade/`

Displayed: supplied_usage

- The supplied usage has multiple forms; their argument relationships need review.
- Generated and supplied syntax differ; review the forms below (including order, grouping and option values).

### Generated candidates

```text
riak upgrade <version> [--no-permanent]
```

### Supplied syntax

```text
Usage: riak upgrade {version}
Usage: riak upgrade [VERSION]
```

## shell:riak versions

Page: `reference/commands/riak/versions/`

Displayed: generated

- No supplied usage is available for comparison.

### Generated candidates

```text
riak versions
```

### Supplied syntax

```text
(not supplied)
```
